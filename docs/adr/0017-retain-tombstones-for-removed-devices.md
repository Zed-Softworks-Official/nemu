# Retain tombstones for removed Devices

Explicit removal ends a Device's active identity but retains a Device tombstone. If a trustworthy protocol identity returns, Nemu offers to restore the Device's metadata and history. Nemu never merges Devices automatically by name, model, or Room.
