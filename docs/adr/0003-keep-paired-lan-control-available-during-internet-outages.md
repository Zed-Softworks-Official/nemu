# Keep paired LAN control available during internet outages

Initial Account setup, invitations, certificate issuance, and Relay access may require an internet connection. After an Access Client pairs with a Controller, it can authenticate and control the Home over the home network while cloud services are unavailable. The Expo app provides this internet-independent client path; the Vercel-hosted web dashboard still requires internet to load.
