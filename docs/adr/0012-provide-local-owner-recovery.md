# Provide local Owner recovery

Nemu creates an offline Recovery Key during first-Owner setup. The key and Home-network access can authorize a new Owner Access Client when every existing Access Client is lost. Operating-system access to the Controller provides a final local recovery command. Convex cannot bypass either recovery check.
