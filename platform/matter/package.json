{
    "name": "@nemu/matter",
    "private": true,
    "scripts": {
        "build:rust": "cargo build",
        "fmt:rust": "cargo fmt",
        "clippy": "cargo clippy --all-targets --locked -- -D clippy::correctness -D clippy::suspicious -W clippy::undocumented_unsafe_blocks"
    }
}
